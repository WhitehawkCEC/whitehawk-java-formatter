package example;

public final class MultipleConditionalComplicated {
  int foo(int depth, int generic, Huh tokenClasses, int j) {
    if (
      depth == 0
        && generic == 0
        && (tokenClasses.has(j, Wlassification.CLOSER) || sym == Zym.SEMI || sym == Zym.COMMA)
    ) {
      return j - 1;
    }
  }
}
